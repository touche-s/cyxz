# 后续待办清单

> 最后更新：2026-08-10

## 已完成（2026-08-10）

### P0 架构硬伤修复

- [x] **破除 post ↔ circle 循环依赖**
  - RabbitMQ 事件驱动（PostCountEvent）替代 Feign 批量同步
  - common 新增事件类 + 常量；post 发布事件；circle 消费原子更新计数
  - circle 删除 post-api 依赖、Feign 客户端、定时任务
  - main + test 编译全部通过

- [x] **PostController 拆分 PostAdminController**
  - 5 个平台 admin 接口迁入 `PostAdminController`（`/admin/post` 前缀）
  - 圈子审核（圈主 C 端能力）保留原处
  - 网关新增 `/api/admin/post/**` 路由

- [x] **网关统一 `/api/admin/**` 路由隔离**（post / governance 已完成）

### P1 governance 治理中心

- [x] **举报主线**
  - 用户端 `POST /api/report` 提交举报
  - 管理端 `/api/admin/report` 列表/详情/通过/驳回
  - 审核通过发布 `ContentTakedownEvent`
  - 建表 `report`（uk_reporter_target 防重复举报）

- [x] **建圈/入圈申请已迁入 circle 模块**（同步事务替代 MQ 事件）

---

## 待办

### P1 高优先级

- [ ] **post/comment 消费 ContentTakedownEvent**
  - 举报通过后自动删除/隐藏对应内容
  - post 消费 POST 类型事件，comment 消费 COMMENT 类型事件
  - 涉及：post/comment 新增 RabbitConfig + Consumer，调用各自的删除逻辑
  - 预计工作量：2 个消费端 + 2 套 MQ 配置

- [ ] **message 通知举报结果**
  - 举报通过 → 通知举报人（已处理）+ 通知内容作者（内容被删）
  - 举报驳回 → 通知举报人（未通过）
  - 涉及：message 消费 ContentTakedownEvent + governance 发通知事件

### P0 中优先级

- [ ] **其他服务 AdminController 拆分跟进**
  - comment / circle / user 等服务剥离 admin 接口到 `*AdminController`
  - 网关同步加 `/api/admin/**` 路由
  - 参照 PostController 拆分模式

- [ ] **补全权限码到 sys_permission 表**
  - `report:review:list` / `report:review:approve` / `report:review:reject`
  - `circle:application:review:list` / `:approve` / `:reject`
  - `circle:join:review:list` / `:approve` / `:reject`
  - 并关联到对应角色（sys_role_permission）

### P2 中优先级（架构亮点）

- [ ] **cyxz-audit 事件驱动审计**
  - 消费 ADMIN_OP 事件，幂等写入审计日志
  - 后台按人/类型/时间查询
  - 注意：先称"事件驱动审计"，Outbox 成熟后再称"事件溯源"
  - 涉及：新建 cyxz-audit 模块 + audit-api

- [ ] **cyxz-analytics CQRS 读模型**
  - 消费业务事件，日维度物化统计
  - 看板查本地读模型，避免 Feign 扇出
  - 第一版：用户日注册数、帖子日发布数、审核通过/拒绝数、圈子成员增长数
  - 涉及：新建 cyxz-analytics 模块 + analytics-api

### P3 低优先级（暂缓）

- [ ] cyxz-admin-bff（等出现 4+ 服务聚合需求）
- [ ] cyxz-ops-config（敏感词/AI策略/公告/全局参数，等配置增多）
- [ ] cyxz-chat / cyxz-notify 拆分（等规模扩大）
- [ ] Zipkin 链路追踪
- [ ] 复杂 Saga 补偿机制

---

## 下一步建议

最高优先级：**接通举报事件链**（post/comment 消费 ContentTakedownEvent + message 通知），让举报闭环真正跑通。这是 governance 三条主线中唯一还没接通消费端的一条。
