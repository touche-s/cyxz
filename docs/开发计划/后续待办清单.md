# 后续待办清单

> 最后更新：2026-08-12

## 已完成（2026-08-10 ~ 2026-08-12）

### P0 架构硬伤修复

- [x] **破除 post ↔ circle 循环依赖**
  - RabbitMQ 事件驱动（PostCountEvent）替代 Feign 批量同步
  - common 新增事件类 + 常量；post 发布事件；circle 消费原子更新计数
  - circle 删除 post-api 依赖、Feign 客户端、定时任务

- [x] **PostController 拆分 PostAdminController**
  - 5 个平台 admin 接口迁入 `PostAdminController`（`/admin/post` 前缀）
  - 圈子审核（圈主 C 端能力）保留原处
  - 网关新增 `/api/admin/post/**` 路由

- [x] **网关统一 `/api/admin/**` 路由隔离**
  - post / governance / audit / analytics / circle-application / circle-join-application / section-template 已完成
  - 08-12 补充 `/api/admin/circle/**` 路由

- [x] **CircleController 拆分 CircleAdminController**
  - 4 个平台管理接口迁入 CircleAdminController：adminList / create / delete / updateStatus
  - CircleController 保留 C 端查询、加入/退出、成员管理、板块配置

- [x] **补全权限码到 sys_permission 表**
  - `report:review:list/approve/reject` (23-25)
  - `circle:application:review:list/approve/reject` (26-28)
  - `circle:join:review:list/approve/reject` (29-31)
  - 站主(1) 全量 31 权限、平台管理员(2) 25 权限
  - 本地 MySQL + Docker MySQL 均已同步

### P1 governance 治理中心

- [x] **举报主线**
  - 用户端 `POST /api/report` 提交举报
  - 管理端 `/api/admin/report` 列表/详情/通过/驳回
  - 审核通过发布 `ContentTakedownEvent`
  - 建表 `report`（uk_reporter_target 防重复举报）

- [x] **建圈/入圈申请已迁入 circle 模块**（同步事务替代 MQ 事件）

- [x] **post/comment 消费 ContentTakedownEvent**
  - PostTakedownConsumer：举报通过后 adminDeletePost 软删除 + 通知帖子作者/举报人
  - CommentTakedownConsumer：举报通过后 adminDeleteComment 逻辑删除 + 通知评论作者/举报人
  - 均有死信队列兜底

- [x] **message 通知举报结果**
  - POST_TAKEDOWN / COMMENT_TAKEDOWN / REPORT_RESULT 通知类型已就绪
  - 举报通过 → 通知内容作者 + 举报人，举报驳回 → 通知举报人

### P2 架构亮点

- [x] **cyxz-audit 事件驱动审计**
  - 模块已建（端口 9007），消息消费 + 幂等落库 + 后台分页查询
  - 08-12 补齐各业务服务 AuditEvent 发布点：UserAdmin(disable/enable)、Circle(create/delete)、CircleApplication(approve/reject)、CircleJoinApplication(approve/reject)、PostReview(approve/reject)
  - Gateway 已有 `/api/admin/audit/**` 路由

- [x] **cyxz-analytics CQRS 读模型**
  - 模块已建（端口 9008），消费 AnalyticsEvent UPSERT 到 daily_statistic
  - 08-12 补齐统计事件发布点：Auth(register→NEW_USER)、Post(create→NEW_POST, approve/reject→POST_APPROVED/REJECTED)、Circle(create→NEW_CIRCLE, join→NEW_JOIN)、Report(handle→REPORT_HANDLED)
  - 避免建圈/入圈申请审批路径重复计数（统一由核心方法发布）
  - AnalyticsAdminController 加 @PreAuthorize
  - Gateway 已有 `/api/admin/analytics/**` 路由

### 工程审查 Critical/High 修复

- [x] **C1+H1 RestTemplate 超时 + commonPool 雪崩** — connectTimeout=2s, readTimeout=10s; 所有 CompletableFuture 使用自定义线程池
- [x] **H6-H8 SITE_OWNER 保护缺失** — guardSiteOwner() + built_in 角色禁止修改权限 + 仅站主可分配站主角色
- [x] **H3 权限缓存删除在 @Transactional 内** — TransactionUtils.afterCommit() 统一处理
- [x] **H4 hardDeletePost ES 同步违反 afterCommit** — TransactionSynchronization.afterCommit()
- [x] **H10 PostEsSyncService publishCountEvent 吞异常** — 补充 Redis 失败队列 + 定时重试（与 ES 同步同级兜底）

### 08-12 新增优化

- [x] **消费端幂等保护** — Analytics ON DUPLICATE KEY UPDATE、Audit event_id 唯一键防重、PostCount Redis SETNX、ES _id 天然幂等
- [x] **logback-spring.xml 合并** — 9 份重复配置收敛为 common 模块公共配置

---

## 待办

### P2 性能优化

### P3 代码规范

### P3 低优先级（暂缓）

- [ ] cyxz-admin-bff（等出现 4+ 服务聚合需求）
- [ ] cyxz-ops-config（敏感词/AI策略/公告/全局参数，等配置增多）
- [ ] cyxz-chat / cyxz-notify 拆分（等规模扩大）
- [ ] S8 接口限流（Sentinel，低优先级）
- [ ] L7 AI 审核超时延迟队列（当前 fail-closed 模式已有兜底）
- [ ] Zipkin 链路追踪
- [ ] 复杂 Saga 补偿机制

---

## 下一步建议

所有审查问题（P0-P3）已全部清零。剩余暂缓项（限流、Zipkin、Saga 等）可根据实际规模需要按需推进。
